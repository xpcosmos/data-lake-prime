useradd -m hadoop
passwd hadoop

# rename jdk-xxx-xxx to jdk
# move jdk to /opt/jdk

PATH=$PATH:$HOME/bin
export PATH
export JAVA_HOME=/opt/jdk
export JRE_HOME=/opt/jdk/jre
export PATH=$PATH:$JAVA_HOME/bin:$JRE_HOME/bin


tar xzf jdk-8u202-linux-arm64-vfp-hflt.tar.gz
mv jdk1.8.0_202 jdk
vi .bash_profile
source .bash_profile
source .bash_profile
vi .bash_profile
source .bash_profile
ls
